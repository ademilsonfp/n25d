var proto = obj_prop('prototype'),
    arr_proto = proto(Array),
    canvas_proto = proto(HTMLCanvasElement),
    canvas_ctx_proto = proto(CanvasRenderingContext2D),
    dom_el_proto = proto(HTMLElement),
    dom_ev_proto = proto(EventTarget);
